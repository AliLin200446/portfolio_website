# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# PHOTO-QUICKHANG: 原图源目录不进仓库/不上线;public/photography 由
# scripts/derive-photos.mjs 派生(WebP,零 EXIF)
/photography/

# raw demo masters — never in git (GitHub caps files at 100MB)
*.mov
latent/demo/
material_memory/demo.mov

.next-build

# stray screenshots dropped at the repo root
/workbench.png

# raw captures for /experiments. The derived 16:10 webp lives in
# public/experiments; these are 11 MB of PNG and stay out of history,
# the way photography originals should have.
/experiments/

# stray copy of the brand source; the real one is logo/alilinlab-logo.png
/logo.png
